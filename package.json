{
  "name": "live-editor",
  "private": true,
  "version": "0.2.0",
  "description": "Hosting for the MCP Toolkit Editor at editor.mcpdesc.org — consumes a portable editor build and overlays hosting concerns (security headers + optional analytics).",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npm run build && npx --yes serve dist",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@cisco_open/mcptoolkit-editor-dist": "^1.1.0-rc.2"
  }
}
